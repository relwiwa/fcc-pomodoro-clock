import React from 'react';

const PomodoroTimer = (props) => {
  const { remainingTime, timerLabel } = props;

  return (
    <div className="pomodoro-timer">

    </div>
  );
};

export default PomodoroTimer;
